 ps : >>>is used to check the process in the snap type which esay and srip
 systemctl : >>>>status , start,stop,enable,
 all this are use for the  to  a procees for the  its life type 
 top-h : >>>>is every use full one which shows the live monitoring the process
 kill ("psid") : >>>>is use to the kill a proces using the id 
 note(" is there is a sudden high use of the cpu frist we need to check the ps and df-h to understand the stuiation")
 file_system
 the file system is the one of the most imp one  comments in the linux 
  ls -l : >>>>>is used to see the file permison of the files and the dir and also shows the user grps and also the memory used in bytes and also the time when ist been created 
  chmod :>>>>> is used to give and change the permison for the files and the dir  
  >cat : >>>>is used the read wt is inside the file
  >write : >>>>vim ,nano,vi all this are use to write inside the file 
  >execute permison (.sh ,.py)
 networking
>ping : >>>>is used check is that its woeking properly and the pages are comming perfectly
>ip addr : >>>>is usede to sghow your ip adress
>ipconfig : >>>is also used for the ip show
>nslookup
curl : >>>>make sure u have installed is apt install curl : its used foir the hhtps ,http,scd kind ahh pull

